# Keep the value single-quoted so the '$' characters remain literal.
# Generate it with: printf '%s\n' 'your access password' | docker run --rm -i IMAGE hash-password
AEGISUB_COLLAB_ACCESS_PASSWORD_HASH='$argon2id$v=19$m=65536,t=3,p=2$REPLACE_WITH_GENERATED_HASH'
